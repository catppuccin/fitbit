#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/js$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/js$exe"  "$basedir/../source-map-compactor/cli" $args
  } else {
    & "$basedir/js$exe"  "$basedir/../source-map-compactor/cli" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "js$exe"  "$basedir/../source-map-compactor/cli" $args
  } else {
    & "js$exe"  "$basedir/../source-map-compactor/cli" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
